_G.describe = function(a, b) end
_G.it = function(a, b) end

_G.assert = {
    are = {
        equal = function(a, b) end,
        same = function(a, b) end,
        not_equal = function(a, b) end
    },
}
